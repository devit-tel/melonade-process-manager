export const adminClient = jest.fn();

export const stateConsumerClient = jest.fn();

export const commandConsumerClient = jest.fn();

export const producerClient = jest.fn();

export const isHealthy = jest.fn();

export const createTopic = jest.fn();

export const createTaskTopic = jest.fn();

export const poll = jest.fn();

export const sendTimer = jest.fn();

export const dispatch = jest.fn();

export const sendUpdate = jest.fn();

export const sendEvent = jest.fn();

export const flush = jest.fn();
